# weekly

One folder per week: `week-01/`, `week-02/`, etc. Each week folder that contains
a project must have its own `project-brief.md` with a filled-in OUTCOME field
before any code is written — see `Projects/README.md`.
